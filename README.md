# CLI Weather Reporter

A simple Python script that fetches current weather data for any city using the [wttr.in](https://wttr.in) service. It parses the JSON response and outputs a clean, formatted text report.

### Features
- No API key required.
- Displays Condition, Temperature (°C), Humidity, and Wind Speed.
- Clean ASCII output format.

### Prerequisites
- Python 3
- `requests` library

### Setup & Usage

1. **Install the dependency:**
   ```bash
   pip install requests

2. **Run the Script
   - python Weather_Check
  
3. ** Enter the city name when prompted
